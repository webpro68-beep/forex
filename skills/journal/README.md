# Journal Skills

This package contains tools for recording trades, calculating statistics, and exporting journal data.

Modules:
- `save_trade.py` — persist a trade entry to a journal list.
- `trade_statistics.py` — compute win rate and average return.
- `export_journal.py` — export journal data to CSV.
- `schemas.py` — trade entry data model.
