# Execution Skills

This package contains helpers for building order payloads and modifying orders.

Modules:
- `open_order.py` — create open order payloads.
- `close_order.py` — create close order payloads.
- `modify_order.py` — build order change requests.
- `schemas.py` — order payload data classes.
